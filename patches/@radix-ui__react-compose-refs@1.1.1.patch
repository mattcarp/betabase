diff --git a/dist/index.mjs b/dist/index.mjs
index 46b15c2b37784f1c681869f17f019ed028dbd9db..ab69714b49b1450434a58c1d7c1fbea7efa7af48 100644
--- a/dist/index.mjs
+++ b/dist/index.mjs
@@ -1,12 +1,29 @@
 // packages/react/compose-refs/src/composeRefs.tsx
+// PATCHED: Added recursion guard for React 19 compatibility
 import * as React from "react";
+
+// Track recursion depth to prevent infinite loops
+let recursionDepth = 0;
+const MAX_RECURSION_DEPTH = 50;
+
 function setRef(ref, value) {
-  if (typeof ref === "function") {
-    return ref(value);
-  } else if (ref !== null && ref !== void 0) {
-    ref.current = value;
+  if (recursionDepth >= MAX_RECURSION_DEPTH) {
+    console.warn('[compose-refs] Max recursion depth exceeded, skipping ref assignment');
+    return;
+  }
+
+  recursionDepth++;
+  try {
+    if (typeof ref === "function") {
+      return ref(value);
+    } else if (ref !== null && ref !== void 0) {
+      ref.current = value;
+    }
+  } finally {
+    recursionDepth--;
   }
 }
+
 function composeRefs(...refs) {
   return (node) => {
     let hasCleanup = false;
@@ -31,9 +48,11 @@ function composeRefs(...refs) {
     }
   };
 }
+
 function useComposedRefs(...refs) {
   return React.useCallback(composeRefs(...refs), refs);
 }
+
 export {
   composeRefs,
   useComposedRefs
