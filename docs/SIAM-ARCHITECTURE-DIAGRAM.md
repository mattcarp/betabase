# SIAM Architecture Diagram for Excalidraw

## System Overview: Chat Query Flow with Vector Search Optimization

This diagram shows the complete flow from user query to AI response, highlighting the performance optimizations implemented on 2025-10-28.

---

## Components Layout

### User Interface Layer (Top)
```
┌─────────────────────────────────────────────────────────────┐
│                     USER BROWSER                             │
│  ┌──────────────────────────────────────────────────────┐   │
│  │  Chat Interface (Vercel AI SDK v5)                   │   │
│  │  - Message input                                      │   │
│  │  - AI Elements: <Response>, <Message>                │   │
│  │  - Real-time streaming                               │   │
│  │  - Voice input/output (ElevenLabs)                   │   │
│  └──────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────┘
                           │
                           │ POST /api/chat
                           ▼
```

### Application Server Layer (Middle)
```
┌─────────────────────────────────────────────────────────────┐
│              NEXT.JS 15.5.6 (Render.com)                     │
│  thebetabase.com                                             │
│                                                               │
│  ┌─────────────────────────────────────────────────────┐    │
│  │  /app/api/chat/route.ts                             │    │
│  │  - Authentication check                              │    │
│  │  - Query normalization                               │    │
│  │  - Cache lookup (Redis-style)                        │    │
│  │  - Stream response to client                         │    │
│  └─────────────────────────────────────────────────────┘    │
│                           │                                   │
│                           │                                   │
│  ┌─────────────────────────────────────────────────────┐    │
│  │  AOMA Orchestrator (src/services/aomaOrchestrator)  │    │
│  │  - Parallel query execution                          │    │
│  │  - 15-second timeout                                 │    │
│  │  - Source prioritization                             │    │
│  │  - Response synthesis                                │    │
│  └─────────────────────────────────────────────────────┘    │
│               │                          │                    │
│               │                          │                    │
│        ┌──────┴─────┐            ┌──────┴──────┐            │
│        ▼            ▼            ▼             ▼            │
│   ┌─────────┐  ┌─────────┐  ┌─────────┐  ┌─────────┐      │
│   │ Vector  │  │ Railway │  │ OpenAI  │  │ Jira/   │      │
│   │ Search  │  │   MCP   │  │   API   │  │Confluen│      │
│   │ Service │  │ Service │  │         │  │  ce     │      │
│   └─────────┘  └─────────┘  └─────────┘  └─────────┘      │
└─────────────────────────────────────────────────────────────┘
      │              │
      │              │
      ▼              ▼
```

### Data Layer (Bottom)
```
┌─────────────────────────┐    ┌──────────────────────────┐
│   SUPABASE POSTGRES     │    │   RAILWAY MCP SERVER     │
│   (kfxetwuuzljhybfgmpuc)│    │   (aoma-mesh-mcp)        │
│                         │    │                          │
│  ┌──────────────────┐  │    │  - AOMA documentation    │
│  │ aoma_unified_    │  │    │  - Confluence content    │
│  │ vectors          │  │    │  - Jira tickets          │
│  │                  │  │    │  - Real-time AOMA data   │
│  │ • 28 rows        │  │    └──────────────────────────┘
│  │ • embeddings[    │  │
│  │   1536]          │  │
│  │ • source_type:   │  │
│  │   "knowledge"    │  │
│  └──────────────────┘  │
│                         │
│  ┌──────────────────┐  │
│  │ match_aoma_      │  │
│  │ vectors()        │  │
│  │                  │  │
│  │ • pgvector       │  │
│  │ • cosine         │  │
│  │   distance       │  │
│  │ • threshold:     │  │
│  │   0.50 (50%)     │  │
│  └──────────────────┘  │
└─────────────────────────┘
```

---

## Detailed Query Flow Diagram

### 1. Fast Path (Vector Search Success) - 2-3 seconds
```
User Query: "What is AOMA?"
    │
    ▼
┌───────────────────────────────────────────────────┐
│ 1. Normalize query                                │
│    "what is aoma" → "vector:what is aoma:..."     │
└───────────────────────────────────────────────────┘
    │
    ▼
┌───────────────────────────────────────────────────┐
│ 2. Check cache                                    │
│    Cache key: "vector:what is aoma:knowledge..."  │
│    Result: MISS (first query)                     │
└───────────────────────────────────────────────────┘
    │
    ▼
┌───────────────────────────────────────────────────┐
│ 3. Generate embedding (OpenAI)                    │
│    text-embedding-3-small                         │
│    [0.123, -0.456, 0.789, ...] (1536 dims)        │
│    Time: ~200ms                                   │
└───────────────────────────────────────────────────┘
    │
    ▼
┌───────────────────────────────────────────────────┐
│ 4. Parallel execution (both at once)              │
│    ┌─────────────────────────────────────┐        │
│    │ A. Supabase Vector Search           │        │
│    │    match_aoma_vectors(              │        │
│    │      query_embedding,               │        │
│    │      match_threshold: 0.50,  ✅ FIX │        │
│    │      match_count: 10                │        │
│    │    )                                │        │
│    │    Time: 1.5-2.5s                   │        │
│    │    Results: 5 documents             │        │
│    │    Best similarity: 59.5%           │        │
│    └─────────────────────────────────────┘        │
│    ┌─────────────────────────────────────┐        │
│    │ B. Railway MCP (parallel backup)    │        │
│    │    Time: 2-3ms (cached)             │        │
│    └─────────────────────────────────────┘        │
└───────────────────────────────────────────────────┘
    │
    ▼
┌───────────────────────────────────────────────────┐
│ 5. Use vector results (winner!)                   │
│    Context from Supabase vectors                  │
│    5 relevant AOMA documents                      │
└───────────────────────────────────────────────────┘
    │
    ▼
┌───────────────────────────────────────────────────┐
│ 6. Generate AI response                           │
│    gpt-4o with o1-reasoning                       │
│    Stream tokens back to client                   │
│    Time: ~10-12s                                  │
└───────────────────────────────────────────────────┘
    │
    ▼
┌───────────────────────────────────────────────────┐
│ 7. Cache result                                   │
│    Store for future identical queries             │
│    Next query: <500ms (cache hit)                 │
└───────────────────────────────────────────────────┘

TOTAL TIME: ~14 seconds (first query)
TOTAL TIME: <500ms (cached query)
```

### 2. Slow Path (Vector Search Fails) - 30+ seconds
```
User Query: "Complex AOMA workflow question"
    │
    ▼
┌───────────────────────────────────────────────────┐
│ 1-3. Same as Fast Path                            │
│      (normalize, cache check, generate embedding) │
└───────────────────────────────────────────────────┘
    │
    ▼
┌───────────────────────────────────────────────────┐
│ 4. Parallel execution                             │
│    ┌─────────────────────────────────────┐        │
│    │ A. Supabase Vector Search           │        │
│    │    Returns 0 results                │        │
│    │    (no documents match query)       │        │
│    │    Time: 1.5s                       │        │
│    └─────────────────────────────────────┘        │
│    ┌─────────────────────────────────────┐        │
│    │ B. Railway MCP                      │        │
│    │    Complex query requires API calls │        │
│    │    Time: 15-25 seconds ⚠️           │        │
│    └─────────────────────────────────────┘        │
└───────────────────────────────────────────────────┘
    │
    ▼
┌───────────────────────────────────────────────────┐
│ 5. Orchestrator timeout (15s)                     │
│    Falls back to Railway MCP result               │
└───────────────────────────────────────────────────┘
    │
    ▼
┌───────────────────────────────────────────────────┐
│ 6. Generate AI response                           │
│    Using Railway MCP context                      │
│    Time: ~10s                                     │
└───────────────────────────────────────────────────┘

TOTAL TIME: ~30 seconds
```

---

## Performance Optimization Timeline

### Before Fix (2025-10-27)
```
┌─────────────────────────────────────────────────┐
│ Problem: Threshold too high (0.78 = 78%)       │
│                                                 │
│ Query → Embedding → Vector Search → 0 results  │
│                                    (59.5% < 78%)│
│         ↓                                       │
│    Railway MCP (15-25s) → Response              │
│                                                 │
│ RESULT: 30+ seconds for every query            │
└─────────────────────────────────────────────────┘
```

### After Fix (2025-10-28)
```
┌─────────────────────────────────────────────────┐
│ Solution: Lower threshold to 0.50 (50%)        │
│                                                 │
│ Query → Embedding → Vector Search → 5 results ✅│
│                                    (59.5% > 50%)│
│         ↓                                       │
│    Use vector results → Response                │
│                                                 │
│ RESULT: 14s first query, <500ms cached         │
│ IMPROVEMENT: 53% faster + caching works         │
└─────────────────────────────────────────────────┘
```

---

## Data Flow Annotations for Excalidraw

### Color Coding Suggestions
- **🟢 Green**: Fast path (vector search success)
- **🔴 Red**: Slow path (vector search fails)
- **🔵 Blue**: Data storage/databases
- **🟡 Yellow**: External APIs (OpenAI, Railway)
- **🟣 Purple**: Caching layer
- **⚪ Gray**: User interface

### Arrow Types
- **Solid arrow** →: Direct synchronous call
- **Dashed arrow** ⇢: Async/parallel execution
- **Thick arrow** ⟹: Data flow with payload
- **Double arrow** ⇄: Bidirectional communication

### Icons to Add
- 🔍 Search/Query
- 🧠 AI/Embeddings
- 💾 Database
- ⚡ Fast path
- 🐌 Slow path
- 📦 Cache
- 🔌 API connection
- ⏱️ Timing metrics

---

## Key Metrics Boxes

Add these metric boxes to the diagram:

```
┌─────────────────────────────┐
│ PERFORMANCE METRICS         │
├─────────────────────────────┤
│ Vector Search: 1.5-2.5s     │
│ Railway MCP: 2ms-25s        │
│ OpenAI Embedding: ~200ms    │
│ AI Response: ~10-12s        │
│ Cache Hit: <500ms           │
│                             │
│ TOTAL (with vectors): ~14s  │
│ TOTAL (cached): <500ms      │
│ TOTAL (fallback): ~30s      │
└─────────────────────────────┘

┌─────────────────────────────┐
│ VECTOR SEARCH CONFIG        │
├─────────────────────────────┤
│ Model: text-embedding-3-    │
│        small                │
│ Dimensions: 1536            │
│ Similarity: Cosine distance │
│ Threshold: 0.50 (50%)       │
│ Results: Up to 10           │
│ Storage: pgvector           │
└─────────────────────────────┘

┌─────────────────────────────┐
│ SUPABASE DATA STATUS        │
├─────────────────────────────┤
│ Table: aoma_unified_vectors │
│ Rows: 28 documents          │
│ Content: AOMA login pages,  │
│          interface text     │
│ All have embeddings ✅      │
│ Source type: knowledge      │
└─────────────────────────────┘
```

---

## Component Details

### AOMA Orchestrator Logic
```
┌──────────────────────────────────────────────┐
│ aomaOrchestrator.ts                          │
├──────────────────────────────────────────────┤
│                                              │
│ 1. Start parallel queries                   │
│    Promise.race([                            │
│      vectorSearch(),                         │
│      railwayMCP()                            │
│    ])                                        │
│                                              │
│ 2. Timeout after 15 seconds                 │
│                                              │
│ 3. Priority order:                           │
│    a. Vector search results (fastest)       │
│    b. Railway MCP (if vectors empty)        │
│    c. Timeout fallback                      │
│                                              │
│ 4. Return best available source             │
│                                              │
└──────────────────────────────────────────────┘
```

### Cache Strategy
```
┌──────────────────────────────────────────────┐
│ Cache Key Generation                         │
├──────────────────────────────────────────────┤
│                                              │
│ Input: "What is AOMA?"                       │
│   ↓                                          │
│ Normalize: lowercase, trim, dedupe spaces   │
│   ↓                                          │
│ Key: "vector:what is aoma:knowledge:..."    │
│                                              │
│ Cache Hit → Return in <500ms                │
│ Cache Miss → Execute full flow              │
│                                              │
└──────────────────────────────────────────────┘
```

---

## Future Improvements (Add as dotted boxes)

```
┌─────────────────────────────────────┐
│ 🔮 FUTURE ENHANCEMENTS              │
├─────────────────────────────────────┤
│                                     │
│ 1. Add more AOMA content            │
│    - User guides                    │
│    - Feature docs                   │
│    - Workflow explanations          │
│                                     │
│ 2. Hybrid search                    │
│    - 50% vector similarity          │
│    - 50% keyword matching           │
│                                     │
│ 3. Improve Railway MCP performance  │
│    - Cache AOMA API responses       │
│    - Reduce network latency         │
│                                     │
│ 4. Redis cache layer                │
│    - Replace in-memory cache        │
│    - Share across instances         │
│                                     │
└─────────────────────────────────────┘
```

---

## How to Use This in Excalidraw

1. **Create main containers** for each layer (User, App Server, Data)
2. **Add component boxes** within each layer
3. **Draw arrows** showing data flow (use different styles for sync/async)
4. **Add color coding** based on performance (green=fast, red=slow)
5. **Include metric boxes** with timing data
6. **Add decision diamonds** for orchestrator logic
7. **Use groups** to show parallel execution
8. **Add annotations** explaining key optimizations

### Recommended Layout
- **Horizontal**: Time flows left to right
- **Vertical**: Layers stack top to bottom (User → App → Data)
- **Side panels**: Metrics, config, future improvements

### Interactive Elements
- Add **checkmarks** ✅ for successful paths
- Add **warning icons** ⚠️ for slow paths
- Add **numbers** ①②③ for sequence
- Use **dashed borders** for optional/fallback paths

---

**File saved for import into Excalidraw**
**Last updated**: 2025-10-28
